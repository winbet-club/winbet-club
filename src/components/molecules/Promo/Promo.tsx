import React from 'react';
import styled from 'styled-components';

interface IProps {
  
}

export const Promo = (
  {  }: IProps
) => (
<Wrapper>
  Text
</Wrapper>
);

const Wrapper = styled.div`

`;
 